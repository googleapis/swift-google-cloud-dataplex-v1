// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Action represents an issue requiring administrator action for resolution.
public struct Action: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// The category of issue associated with the action.
  public var category: Action.Category = Action.Category()

  /// Detailed description of the issue requiring action.
  public var issue: Swift.String = Swift.String()

  /// The time that the issue was detected.
  public var detectTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The relative resource name of the action, of the form:
  /// `projects/{project}/locations/{location}/lakes/{lake}/actions/{action}`
  /// `projects/{project}/locations/{location}/lakes/{lake}/zones/{zone}/actions/{action}`
  /// `projects/{project}/locations/{location}/lakes/{lake}/zones/{zone}/assets/{asset}/actions/{action}`.
  public var name: Swift.String = Swift.String()

  /// Output only. The relative resource name of the lake, of the form:
  /// `projects/{project_number}/locations/{location_id}/lakes/{lake_id}`.
  public var lake: Swift.String = Swift.String()

  /// Output only. The relative resource name of the zone, of the form:
  /// `projects/{project_number}/locations/{location_id}/lakes/{lake_id}/zones/{zone_id}`.
  public var zone: Swift.String = Swift.String()

  /// Output only. The relative resource name of the asset, of the form:
  /// `projects/{project_number}/locations/{location_id}/lakes/{lake_id}/zones/{zone_id}/assets/{asset_id}`.
  public var asset: Swift.String = Swift.String()

  /// The list of data locations associated with this action. Cloud Storage
  /// locations are represented as URI paths(E.g.
  /// `gs://bucket/table1/year=2020/month=Jan/`). BigQuery locations refer to
  /// resource names(E.g.
  /// `bigquery.googleapis.com/projects/project-id/datasets/dataset-id`).
  public var dataLocations: [Swift.String] = []

  /// Additional details about the action based on the action category.
  public var details: OneOf_Details? = nil

  /// Initialize a new instance of `Action`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Action().with { $0.category = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case category = "category"
    case issue = "issue"
    case detectTime = "detectTime"
    case name = "name"
    case lake = "lake"
    case zone = "zone"
    case asset = "asset"
    case dataLocations = "dataLocations"
    case invalidDataFormat = "invalidDataFormat"
    case incompatibleDataSchema = "incompatibleDataSchema"
    case invalidDataPartition = "invalidDataPartition"
    case missingData = "missingData"
    case missingResource = "missingResource"
    case unauthorizedResource = "unauthorizedResource"
    case failedSecurityPolicyApply = "failedSecurityPolicyApply"
    case invalidDataOrganization = "invalidDataOrganization"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.category = try container.decode(Action.Category.self, forKey: .category)
    self.issue = try container.decode(Swift.String.self, forKey: .issue)
    self.detectTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .detectTime)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.lake = try container.decode(Swift.String.self, forKey: .lake)
    self.zone = try container.decode(Swift.String.self, forKey: .zone)
    self.asset = try container.decode(Swift.String.self, forKey: .asset)
    self.dataLocations = try container.decode([Swift.String].self, forKey: .dataLocations)

    var details: OneOf_Details? = nil
    let detailsCheckAndSet = {
      if details != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'details'"))
      }
      details = $0
    }
    if let invalidDataFormat = try container.decodeIfPresent(
      Action.InvalidDataFormat?.self, forKey: .invalidDataFormat)
    {
      try detailsCheckAndSet(.invalidDataFormat(invalidDataFormat))
    }
    if let incompatibleDataSchema = try container.decodeIfPresent(
      Action.IncompatibleDataSchema?.self, forKey: .incompatibleDataSchema)
    {
      try detailsCheckAndSet(.incompatibleDataSchema(incompatibleDataSchema))
    }
    if let invalidDataPartition = try container.decodeIfPresent(
      Action.InvalidDataPartition?.self, forKey: .invalidDataPartition)
    {
      try detailsCheckAndSet(.invalidDataPartition(invalidDataPartition))
    }
    if let missingData = try container.decodeIfPresent(
      Action.MissingData?.self, forKey: .missingData)
    {
      try detailsCheckAndSet(.missingData(missingData))
    }
    if let missingResource = try container.decodeIfPresent(
      Action.MissingResource?.self, forKey: .missingResource)
    {
      try detailsCheckAndSet(.missingResource(missingResource))
    }
    if let unauthorizedResource = try container.decodeIfPresent(
      Action.UnauthorizedResource?.self, forKey: .unauthorizedResource)
    {
      try detailsCheckAndSet(.unauthorizedResource(unauthorizedResource))
    }
    if let failedSecurityPolicyApply = try container.decodeIfPresent(
      Action.FailedSecurityPolicyApply?.self, forKey: .failedSecurityPolicyApply)
    {
      try detailsCheckAndSet(.failedSecurityPolicyApply(failedSecurityPolicyApply))
    }
    if let invalidDataOrganization = try container.decodeIfPresent(
      Action.InvalidDataOrganization?.self, forKey: .invalidDataOrganization)
    {
      try detailsCheckAndSet(.invalidDataOrganization(invalidDataOrganization))
    }
    self.details = details
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.category, forKey: .category)
    try container.encode(self.issue, forKey: .issue)
    try container.encode(self.detectTime, forKey: .detectTime)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.lake, forKey: .lake)
    try container.encode(self.zone, forKey: .zone)
    try container.encode(self.asset, forKey: .asset)
    try container.encode(self.dataLocations, forKey: .dataLocations)

    if let choice = self.details {
      switch choice {
      case .invalidDataFormat(let value):
        try container.encode(value, forKey: .invalidDataFormat)
      case .incompatibleDataSchema(let value):
        try container.encode(value, forKey: .incompatibleDataSchema)
      case .invalidDataPartition(let value):
        try container.encode(value, forKey: .invalidDataPartition)
      case .missingData(let value):
        try container.encode(value, forKey: .missingData)
      case .missingResource(let value):
        try container.encode(value, forKey: .missingResource)
      case .unauthorizedResource(let value):
        try container.encode(value, forKey: .unauthorizedResource)
      case .failedSecurityPolicyApply(let value):
        try container.encode(value, forKey: .failedSecurityPolicyApply)
      case .invalidDataOrganization(let value):
        try container.encode(value, forKey: .invalidDataOrganization)
      }
    }
  }

  /// Action details for resource references in assets that cannot be located.
  public struct MissingResource: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `MissingResource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = MissingResource().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.MissingResource"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for unauthorized resource issues raised to indicate that the
  /// service account associated with the lake instance is not authorized to
  /// access or manage the resource associated with an asset.
  public struct UnauthorizedResource: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `UnauthorizedResource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = UnauthorizedResource().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.UnauthorizedResource"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Failed to apply security policy to the managed resource(s) under a
  /// lake, zone or an asset. For a lake or zone resource, one or more underlying
  /// assets has a failure applying security policy to the associated managed
  /// resource.
  public struct FailedSecurityPolicyApply: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Resource name of one of the assets with failing security policy
    /// application. Populated for a lake or zone resource only.
    public var asset: Swift.String = Swift.String()

    /// Initialize a new instance of `FailedSecurityPolicyApply`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = FailedSecurityPolicyApply().with { $0.asset = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.FailedSecurityPolicyApply"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for invalid or unsupported data files detected by discovery.
  public struct InvalidDataFormat: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The list of data locations sampled and used for format/schema
    /// inference.
    public var sampledDataLocations: [Swift.String] = []

    /// The expected data format of the entity.
    public var expectedFormat: Swift.String = Swift.String()

    /// The new unexpected data format within the entity.
    public var newFormat: Swift.String = Swift.String()

    /// Initialize a new instance of `InvalidDataFormat`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = InvalidDataFormat().with { $0.sampledDataLocations = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.InvalidDataFormat"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for incompatible schemas detected by discovery.
  public struct IncompatibleDataSchema: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The name of the table containing invalid data.
    public var table: Swift.String = Swift.String()

    /// The existing and expected schema of the table. The schema is provided as
    /// a JSON formatted structure listing columns and data types.
    public var existingSchema: Swift.String = Swift.String()

    /// The new and incompatible schema within the table. The schema is provided
    /// as a JSON formatted structured listing columns and data types.
    public var newSchema: Swift.String = Swift.String()

    /// The list of data locations sampled and used for format/schema
    /// inference.
    public var sampledDataLocations: [Swift.String] = []

    /// Whether the action relates to a schema that is incompatible or modified.
    public var schemaChange: Action.IncompatibleDataSchema.SchemaChange = Action
      .IncompatibleDataSchema.SchemaChange()

    /// Initialize a new instance of `IncompatibleDataSchema`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = IncompatibleDataSchema().with { $0.table = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Whether the action relates to a schema that is incompatible or modified.
    public enum SchemaChange: Codable, Equatable, Sendable {
      /// Schema change unspecified.
      case unspecified
      /// Newly discovered schema is incompatible with existing schema.
      case incompatible
      /// Newly discovered schema has changed from existing schema for data in a
      /// curated zone.
      case modified
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .incompatible: return 1
        case .modified: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SCHEMA_CHANGE_UNSPECIFIED"
        case .incompatible: return "INCOMPATIBLE"
        case .modified: return "MODIFIED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SchemaChange/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SCHEMA_CHANGE_UNSPECIFIED": self = .unspecified
        case "INCOMPATIBLE": self = .incompatible
        case "MODIFIED": self = .modified
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SchemaChange/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .incompatible
        case 2: self = .modified
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .incompatible: return try container.encode(1)
        case .modified: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.IncompatibleDataSchema"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for invalid or unsupported partitions detected by discovery.
  public struct InvalidDataPartition: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The issue type of InvalidDataPartition.
    public var expectedStructure: Action.InvalidDataPartition.PartitionStructure = Action
      .InvalidDataPartition.PartitionStructure()

    /// Initialize a new instance of `InvalidDataPartition`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = InvalidDataPartition().with { $0.expectedStructure = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The expected partition structure.
    public enum PartitionStructure: Codable, Equatable, Sendable {
      /// PartitionStructure unspecified.
      case unspecified
      /// Consistent hive-style partition definition (both raw and curated zone).
      case consistentKeys
      /// Hive style partition definition (curated zone only).
      case hiveStyleKeys
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .consistentKeys: return 1
        case .hiveStyleKeys: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "PARTITION_STRUCTURE_UNSPECIFIED"
        case .consistentKeys: return "CONSISTENT_KEYS"
        case .hiveStyleKeys: return "HIVE_STYLE_KEYS"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:PartitionStructure/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "PARTITION_STRUCTURE_UNSPECIFIED": self = .unspecified
        case "CONSISTENT_KEYS": self = .consistentKeys
        case "HIVE_STYLE_KEYS": self = .hiveStyleKeys
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:PartitionStructure/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .consistentKeys
        case 2: self = .hiveStyleKeys
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .consistentKeys: return try container.encode(1)
        case .hiveStyleKeys: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.InvalidDataPartition"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for absence of data detected by discovery.
  public struct MissingData: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `MissingData`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = MissingData().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.MissingData"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Action details for invalid data arrangement.
  public struct InvalidDataOrganization: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `InvalidDataOrganization`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = InvalidDataOrganization().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Action.InvalidDataOrganization"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The category of issues.
  public enum Category: Codable, Equatable, Sendable {
    /// Unspecified category.
    case unspecified
    /// Resource management related issues.
    case resourceManagement
    /// Security policy related issues.
    case securityPolicy
    /// Data and discovery related issues.
    case dataDiscovery
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .resourceManagement: return 1
      case .securityPolicy: return 2
      case .dataDiscovery: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "CATEGORY_UNSPECIFIED"
      case .resourceManagement: return "RESOURCE_MANAGEMENT"
      case .securityPolicy: return "SECURITY_POLICY"
      case .dataDiscovery: return "DATA_DISCOVERY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Category/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "CATEGORY_UNSPECIFIED": self = .unspecified
      case "RESOURCE_MANAGEMENT": self = .resourceManagement
      case "SECURITY_POLICY": self = .securityPolicy
      case "DATA_DISCOVERY": self = .dataDiscovery
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Category/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .resourceManagement
      case 2: self = .securityPolicy
      case 3: self = .dataDiscovery
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .resourceManagement: return try container.encode(1)
      case .securityPolicy: return try container.encode(2)
      case .dataDiscovery: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Additional details about the action based on the action category.
  public enum OneOf_Details: Codable, Equatable, Sendable {
    /// Details for issues related to invalid or unsupported data formats.
    indirect case invalidDataFormat(Action.InvalidDataFormat?)
    /// Details for issues related to incompatible schemas detected within data.
    indirect case incompatibleDataSchema(Action.IncompatibleDataSchema?)
    /// Details for issues related to invalid or unsupported data partition
    /// structure.
    indirect case invalidDataPartition(Action.InvalidDataPartition?)
    /// Details for issues related to absence of data within managed resources.
    indirect case missingData(Action.MissingData?)
    /// Details for issues related to absence of a managed resource.
    indirect case missingResource(Action.MissingResource?)
    /// Details for issues related to lack of permissions to access data
    /// resources.
    indirect case unauthorizedResource(Action.UnauthorizedResource?)
    /// Details for issues related to applying security policy.
    indirect case failedSecurityPolicyApply(Action.FailedSecurityPolicyApply?)
    /// Details for issues related to invalid data arrangement.
    indirect case invalidDataOrganization(Action.InvalidDataOrganization?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.Action"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
