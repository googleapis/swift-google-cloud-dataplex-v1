// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// A rule captures data quality intent about a data source.
public struct DataQualityRule: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Optional. The unnested column which this rule is evaluated against.
  public var column: Swift.String = Swift.String()

  /// Optional. Rows with `null` values will automatically fail a rule, unless
  /// `ignore_null` is `true`. In that case, such `null` rows are trivially
  /// considered passing.
  ///
  /// This field is only valid for the following type of rules:
  ///
  /// * RangeExpectation
  /// * RegexExpectation
  /// * SetExpectation
  /// * UniquenessExpectation
  public var ignoreNull: Swift.Bool = Swift.Bool()

  /// Optional. The dimension a rule belongs to. Results are also aggregated at
  /// the dimension level. Custom dimension name is supported with all uppercase
  /// letters and maximum length of 30 characters.
  public var dimension: Swift.String = Swift.String()

  /// Optional. The minimum ratio of **passing_rows / total_rows** required to
  /// pass this rule, with a range of [0.0, 1.0].
  ///
  /// 0 indicates default value (i.e. 1.0).
  ///
  /// This field is only valid for row-level type rules.
  public var threshold: Swift.Double = Swift.Double()

  /// Optional. A mutable name for the rule.
  ///
  /// * The name must contain only letters (a-z, A-Z), numbers (0-9), or
  /// hyphens (-).
  /// * The maximum length is 63 characters.
  /// * Must start with a letter.
  /// * Must end with a number or a letter.
  public var name: Swift.String = Swift.String()

  /// Optional. Description of the rule.
  ///
  /// * The maximum length is 1,024 characters.
  public var description: Swift.String = Swift.String()

  /// Optional. Whether the Rule is active or suspended.
  /// Default is false.
  public var suspended: Swift.Bool = Swift.Bool()

  /// Optional. Map of attribute name and value linked to the rule. The rules to
  /// evaluate can be filtered based on attributes provided here and a filter
  /// expression provided in the DataQualitySpec.filter field.
  public var attributes: [Swift.String: Swift.String] = [:]

  /// Output only. Contains information about the source of the rule and its
  /// relationship with the BigQuery table, where applicable.
  public var ruleSource: DataQualityRule.RuleSource? = nil

  /// Optional. Specifies the debug queries for this rule.
  /// Currently, only one query is supported, but this may be expanded in the
  /// future.
  public var debugQueries: [DataQualityRule.DebugQuery] = []

  /// The rule-specific configuration.
  public var ruleType: OneOf_RuleType? = nil

  /// Initialize a new instance of `DataQualityRule`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = DataQualityRule().with { $0.rangeExpectation = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case rangeExpectation = "rangeExpectation"
    case nonNullExpectation = "nonNullExpectation"
    case setExpectation = "setExpectation"
    case regexExpectation = "regexExpectation"
    case uniquenessExpectation = "uniquenessExpectation"
    case statisticRangeExpectation = "statisticRangeExpectation"
    case rowConditionExpectation = "rowConditionExpectation"
    case tableConditionExpectation = "tableConditionExpectation"
    case sqlAssertion = "sqlAssertion"
    case templateReference = "templateReference"
    case column = "column"
    case ignoreNull = "ignoreNull"
    case dimension = "dimension"
    case threshold = "threshold"
    case name = "name"
    case description = "description"
    case suspended = "suspended"
    case attributes = "attributes"
    case ruleSource = "ruleSource"
    case debugQueries = "debugQueries"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.column = try container.decode(Swift.String.self, forKey: .column)
    self.ignoreNull = try container.decode(Swift.Bool.self, forKey: .ignoreNull)
    self.dimension = try container.decode(Swift.String.self, forKey: .dimension)
    self.threshold = try container.decode(Swift.Double.self, forKey: .threshold)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.suspended = try container.decode(Swift.Bool.self, forKey: .suspended)
    self.attributes = try container.decode([Swift.String: Swift.String].self, forKey: .attributes)
    self.ruleSource = try container.decodeIfPresent(
      DataQualityRule.RuleSource.self, forKey: .ruleSource)
    self.debugQueries = try container.decode(
      [DataQualityRule.DebugQuery].self, forKey: .debugQueries)

    var ruleType: OneOf_RuleType? = nil
    let ruleTypeCheckAndSet = {
      if ruleType != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'ruleType'"))
      }
      ruleType = $0
    }
    if let rangeExpectation = try container.decodeIfPresent(
      DataQualityRule.RangeExpectation?.self, forKey: .rangeExpectation)
    {
      try ruleTypeCheckAndSet(.rangeExpectation(rangeExpectation))
    }
    if let nonNullExpectation = try container.decodeIfPresent(
      DataQualityRule.NonNullExpectation?.self, forKey: .nonNullExpectation)
    {
      try ruleTypeCheckAndSet(.nonNullExpectation(nonNullExpectation))
    }
    if let setExpectation = try container.decodeIfPresent(
      DataQualityRule.SetExpectation?.self, forKey: .setExpectation)
    {
      try ruleTypeCheckAndSet(.setExpectation(setExpectation))
    }
    if let regexExpectation = try container.decodeIfPresent(
      DataQualityRule.RegexExpectation?.self, forKey: .regexExpectation)
    {
      try ruleTypeCheckAndSet(.regexExpectation(regexExpectation))
    }
    if let uniquenessExpectation = try container.decodeIfPresent(
      DataQualityRule.UniquenessExpectation?.self, forKey: .uniquenessExpectation)
    {
      try ruleTypeCheckAndSet(.uniquenessExpectation(uniquenessExpectation))
    }
    if let statisticRangeExpectation = try container.decodeIfPresent(
      DataQualityRule.StatisticRangeExpectation?.self, forKey: .statisticRangeExpectation)
    {
      try ruleTypeCheckAndSet(.statisticRangeExpectation(statisticRangeExpectation))
    }
    if let rowConditionExpectation = try container.decodeIfPresent(
      DataQualityRule.RowConditionExpectation?.self, forKey: .rowConditionExpectation)
    {
      try ruleTypeCheckAndSet(.rowConditionExpectation(rowConditionExpectation))
    }
    if let tableConditionExpectation = try container.decodeIfPresent(
      DataQualityRule.TableConditionExpectation?.self, forKey: .tableConditionExpectation)
    {
      try ruleTypeCheckAndSet(.tableConditionExpectation(tableConditionExpectation))
    }
    if let sqlAssertion = try container.decodeIfPresent(
      DataQualityRule.SqlAssertion?.self, forKey: .sqlAssertion)
    {
      try ruleTypeCheckAndSet(.sqlAssertion(sqlAssertion))
    }
    if let templateReference = try container.decodeIfPresent(
      DataQualityRule.TemplateReference?.self, forKey: .templateReference)
    {
      try ruleTypeCheckAndSet(.templateReference(templateReference))
    }
    self.ruleType = ruleType
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.column, forKey: .column)
    try container.encode(self.ignoreNull, forKey: .ignoreNull)
    try container.encode(self.dimension, forKey: .dimension)
    try container.encode(self.threshold, forKey: .threshold)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.suspended, forKey: .suspended)
    try container.encode(self.attributes, forKey: .attributes)
    try container.encode(self.ruleSource, forKey: .ruleSource)
    try container.encode(self.debugQueries, forKey: .debugQueries)

    if let choice = self.ruleType {
      switch choice {
      case .rangeExpectation(let value):
        try container.encode(value, forKey: .rangeExpectation)
      case .nonNullExpectation(let value):
        try container.encode(value, forKey: .nonNullExpectation)
      case .setExpectation(let value):
        try container.encode(value, forKey: .setExpectation)
      case .regexExpectation(let value):
        try container.encode(value, forKey: .regexExpectation)
      case .uniquenessExpectation(let value):
        try container.encode(value, forKey: .uniquenessExpectation)
      case .statisticRangeExpectation(let value):
        try container.encode(value, forKey: .statisticRangeExpectation)
      case .rowConditionExpectation(let value):
        try container.encode(value, forKey: .rowConditionExpectation)
      case .tableConditionExpectation(let value):
        try container.encode(value, forKey: .tableConditionExpectation)
      case .sqlAssertion(let value):
        try container.encode(value, forKey: .sqlAssertion)
      case .templateReference(let value):
        try container.encode(value, forKey: .templateReference)
      }
    }
  }

  /// Evaluates whether each column value lies between a specified range.
  public struct RangeExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The minimum column value allowed for a row to pass this
    /// validation. At least one of `min_value` and `max_value` need to be
    /// provided.
    public var minValue: Swift.String = Swift.String()

    /// Optional. The maximum column value allowed for a row to pass this
    /// validation. At least one of `min_value` and `max_value` need to be
    /// provided.
    public var maxValue: Swift.String = Swift.String()

    /// Optional. Whether each value needs to be strictly greater than ('>') the
    /// minimum, or if equality is allowed.
    ///
    /// Only relevant if a `min_value` has been defined. Default = false.
    public var strictMinEnabled: Swift.Bool = Swift.Bool()

    /// Optional. Whether each value needs to be strictly lesser than ('<') the
    /// maximum, or if equality is allowed.
    ///
    /// Only relevant if a `max_value` has been defined. Default = false.
    public var strictMaxEnabled: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `RangeExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RangeExpectation().with { $0.minValue = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RangeExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether each column value is null.
  public struct NonNullExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `NonNullExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = NonNullExpectation().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.NonNullExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether each column value is contained by a specified set.
  public struct SetExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Expected values for the column value.
    public var values: [Swift.String] = []

    /// Initialize a new instance of `SetExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SetExpectation().with { $0.values = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.SetExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether each column value matches a specified regex.
  public struct RegexExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. A regular expression the column value is expected to match.
    public var regex: Swift.String = Swift.String()

    /// Initialize a new instance of `RegexExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RegexExpectation().with { $0.regex = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RegexExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether the column has duplicates.
  public struct UniquenessExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `UniquenessExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = UniquenessExpectation().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.UniquenessExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether the column aggregate statistic lies between a specified
  /// range.
  public struct StatisticRangeExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The aggregate metric to evaluate.
    public var statistic: DataQualityRule.StatisticRangeExpectation.ColumnStatistic =
      DataQualityRule.StatisticRangeExpectation.ColumnStatistic()

    /// Optional. The minimum column statistic value allowed for a row to pass
    /// this validation.
    ///
    /// At least one of `min_value` and `max_value` need to be provided.
    public var minValue: Swift.String = Swift.String()

    /// Optional. The maximum column statistic value allowed for a row to pass
    /// this validation.
    ///
    /// At least one of `min_value` and `max_value` need to be provided.
    public var maxValue: Swift.String = Swift.String()

    /// Optional. Whether column statistic needs to be strictly greater than
    /// ('>') the minimum, or if equality is allowed.
    ///
    /// Only relevant if a `min_value` has been defined. Default = false.
    public var strictMinEnabled: Swift.Bool = Swift.Bool()

    /// Optional. Whether column statistic needs to be strictly lesser than ('<')
    /// the maximum, or if equality is allowed.
    ///
    /// Only relevant if a `max_value` has been defined. Default = false.
    public var strictMaxEnabled: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `StatisticRangeExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = StatisticRangeExpectation().with { $0.statistic = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The list of aggregate metrics a rule can be evaluated against.
    public enum ColumnStatistic: Codable, Equatable, Sendable {
      /// Unspecified statistic type
      case statisticUndefined
      /// Evaluate the column mean
      case mean
      /// Evaluate the column min
      case min
      /// Evaluate the column max
      case max
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .statisticUndefined
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .statisticUndefined: return 0
        case .mean: return 1
        case .min: return 2
        case .max: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .statisticUndefined: return "STATISTIC_UNDEFINED"
        case .mean: return "MEAN"
        case .min: return "MIN"
        case .max: return "MAX"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ColumnStatistic/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATISTIC_UNDEFINED": self = .statisticUndefined
        case "MEAN": self = .mean
        case "MIN": self = .min
        case "MAX": self = .max
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ColumnStatistic/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .statisticUndefined
        case 1: self = .mean
        case 2: self = .min
        case 3: self = .max
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .statisticUndefined: return try container.encode(0)
        case .mean: return try container.encode(1)
        case .min: return try container.encode(2)
        case .max: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.StatisticRangeExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether each row passes the specified condition.
  ///
  /// The SQL expression needs to use [GoogleSQL
  /// syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax)
  /// and should produce a boolean value per row as the result.
  ///
  /// Example: col1 >= 0 AND col2 < 10
  public struct RowConditionExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The SQL expression.
    public var sqlExpression: Swift.String = Swift.String()

    /// Initialize a new instance of `RowConditionExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RowConditionExpectation().with { $0.sqlExpression = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RowConditionExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Evaluates whether the provided expression is true.
  ///
  /// The SQL expression needs to use [GoogleSQL
  /// syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax)
  /// and should produce a scalar boolean result.
  ///
  /// Example: MIN(col1) >= 0
  public struct TableConditionExpectation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The SQL expression.
    public var sqlExpression: Swift.String = Swift.String()

    /// Initialize a new instance of `TableConditionExpectation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TableConditionExpectation().with { $0.sqlExpression = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.TableConditionExpectation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// A SQL statement that is evaluated to return rows that match an invalid
  /// state. If any rows are are returned, this rule fails.
  ///
  /// The SQL statement must use [GoogleSQL
  /// syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax),
  /// and must not contain any semicolons.
  ///
  /// You can use the data reference parameter `${data()}` to reference the
  /// source table with all of its precondition filters applied. Examples of
  /// precondition filters include row filters, incremental data filters, and
  /// sampling. For more information, see [Data reference
  /// parameter](https://cloud.google.com/dataplex/docs/auto-data-quality-overview#data-reference-parameter).
  ///
  /// Example: `SELECT * FROM ${data()} WHERE price < 0`
  public struct SqlAssertion: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The SQL statement.
    public var sqlStatement: Swift.String = Swift.String()

    /// Initialize a new instance of `SqlAssertion`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SqlAssertion().with { $0.sqlStatement = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.SqlAssertion"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// A rule that constructs a SQL statement to evaluate using a rule template
  /// and parameter values. If the constructed statement returns any rows, this
  /// rule fails
  public struct TemplateReference: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Required. The template entry name. Entry must be of EntryType
    /// `projects/dataplex-types/locations/global/entryTypes/data-quality-rule-template`
    /// and contains top-level aspect of AspectType
    /// `projects/dataplex-types/locations/global/aspectTypes/data-quality-rule-template`.
    /// The format is:
    /// `projects/{project_id_or_number}/locations/{location_id}/entryGroups/{entry_group_id}/entries/{entry_id}`
    public var name: Swift.String = Swift.String()

    /// Optional. Provides the map of parameter name and value.
    /// The maximum size of the field is 120KB (encoded as UTF-8).
    public var values: [Swift.String: DataQualityRule.TemplateReference.ParameterValue] = [:]

    /// Output only. The resolved SQL statement generated from the template with
    /// parameters substituted. It is only populated in the result.
    public var resolvedSql: Swift.String = Swift.String()

    /// Output only. The rule template used to resolve the rule. It is only
    /// populated in the result.
    public var ruleTemplate: DataQualityRuleTemplate? = nil

    /// Initialize a new instance of `TemplateReference`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TemplateReference().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Represents a parameter value.
    public struct ParameterValue: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. Represents the string value of the parameter.
      public var value: Swift.String = Swift.String()

      /// Initialize a new instance of `ParameterValue`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ParameterValue().with { $0.value = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.TemplateReference.ParameterValue"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.TemplateReference"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Represents the rule source information from Catalog.
  public struct RuleSource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. Rule path elements represent information about the
    /// individual items in the relationship path between the scan resource and
    /// rule origin in that order.
    public var rulePathElements: [DataQualityRule.RuleSource.RulePathElement] = []

    /// Initialize a new instance of `RuleSource`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = RuleSource().with { $0.rulePathElements = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Path Element represents the direct relationship between the rule origin
    /// (aspects) to the BigQuery Entry. Ordering of the rule relationship will
    /// be maintained such that the first entry in the list is the closest
    /// ancestor (BigQuery table itself). A blank source denotes that the rule is
    /// derived directly from the DataScan itself.
    public struct RulePathElement: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The source type of the rule.
      public var sourceType: OneOf_SourceType? = nil

      /// Initialize a new instance of `RulePathElement`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = RulePathElement().with { $0.entrySource = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case entrySource = "entrySource"
        case entryLinkSource = "entryLinkSource"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        var sourceType: OneOf_SourceType? = nil
        let sourceTypeCheckAndSet = {
          if sourceType != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'sourceType'"))
          }
          sourceType = $0
        }
        if let entrySource = try container.decodeIfPresent(
          DataQualityRule.RuleSource.RulePathElement.EntrySource?.self, forKey: .entrySource)
        {
          try sourceTypeCheckAndSet(.entrySource(entrySource))
        }
        if let entryLinkSource = try container.decodeIfPresent(
          DataQualityRule.RuleSource.RulePathElement.EntryLinkSource?.self, forKey: .entryLinkSource
        ) {
          try sourceTypeCheckAndSet(.entryLinkSource(entryLinkSource))
        }
        self.sourceType = sourceType
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        if let choice = self.sourceType {
          switch choice {
          case .entrySource(let value):
            try container.encode(value, forKey: .entrySource)
          case .entryLinkSource(let value):
            try container.encode(value, forKey: .entryLinkSource)
          }
        }
      }

      /// Entry source represents information about the related source entry.
      public struct EntrySource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Output only. The entry type to represent the current characteristics
        /// of the entry in the form of:
        /// `projects/{project_id_or_number}/locations/{location_id}/entryTypes/{entry-type-id}`.
        public var entryType: Swift.String = Swift.String()

        /// Output only. The entry name in the form of:
        /// `projects/{project_id_or_number}/locations/{location_id}/entryGroups/{entry_group_id}/entries/{entry_id}`
        public var entry: Swift.String = Swift.String()

        /// Output only. The display name of the entry.
        public var displayName: Swift.String = Swift.String()

        /// Initialize a new instance of `EntrySource`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = EntrySource().with { $0.entryType = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RuleSource.RulePathElement.EntrySource"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Entry link source represents information about the entry link.
      public struct EntryLinkSource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Output only. The entry link type to represent the current
        /// relationship between the entry and the next entry in the path.
        /// In the form of:
        /// `projects/{project_id_or_number}/locations/{location_id}/entryLinkTypes/{entry_link_type_id}`
        public var entryLinkType: Swift.String = Swift.String()

        /// Output only. The entry link name in the form of:
        /// `projects/{project_id_or_number}/locations/{location_id}/entryGroups/{entry_group_id}/entryLinks/{entry_link_id}`
        public var entryLink: Swift.String = Swift.String()

        /// Initialize a new instance of `EntryLinkSource`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = EntryLinkSource().with { $0.entryLinkType = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RuleSource.RulePathElement.EntryLinkSource"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The source type of the rule.
      public enum OneOf_SourceType: Codable, Equatable, Sendable {
        /// Output only. Entry source represents information about the related
        /// source entry.
        indirect case entrySource(DataQualityRule.RuleSource.RulePathElement.EntrySource?)
        /// Output only. Entry link source represents information about the entry
        /// link.
        indirect case entryLinkSource(DataQualityRule.RuleSource.RulePathElement.EntryLinkSource?)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RuleSource.RulePathElement"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.RuleSource"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Specifies a SQL statement that is evaluated to return up to 10 scalar
  /// values that are used to debug rules. If the rule fails, the values can help
  /// diagnose the cause of the failure.
  ///
  /// The SQL statement must use [GoogleSQL
  /// syntax](https://cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax),
  /// and must not contain any semicolons.
  ///
  /// You can use the data reference parameter `${data()}` to reference the
  /// source table with all of its precondition filters applied. Examples of
  /// precondition filters include row filters, incremental data filters, and
  /// sampling. For more information, see [Data reference
  /// parameter](https://cloud.google.com/dataplex/docs/auto-data-quality-overview#data-reference-parameter).
  ///
  /// You can also name results with an explicit alias using `[AS] alias`. For
  /// more information, see [BigQuery explicit
  /// aliases](https://docs.cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#explicit_alias_syntax).
  ///
  /// Example: `SELECT MIN(col1) AS min_col1, MAX(col1) AS max_col1 FROM
  /// ${data()}`
  public struct DebugQuery: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Specifies the description of the debug query.
    ///
    /// * The maximum length is 1,024 characters.
    public var description: Swift.String = Swift.String()

    /// Required. Specifies the SQL statement to be executed.
    public var sqlStatement: Swift.String = Swift.String()

    /// Initialize a new instance of `DebugQuery`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DebugQuery().with { $0.description = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule.DebugQuery"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The rule-specific configuration.
  public enum OneOf_RuleType: Codable, Equatable, Sendable {
    /// Row-level rule which evaluates whether each column value lies between a
    /// specified range.
    indirect case rangeExpectation(DataQualityRule.RangeExpectation?)
    /// Row-level rule which evaluates whether each column value is null.
    indirect case nonNullExpectation(DataQualityRule.NonNullExpectation?)
    /// Row-level rule which evaluates whether each column value is contained by
    /// a specified set.
    indirect case setExpectation(DataQualityRule.SetExpectation?)
    /// Row-level rule which evaluates whether each column value matches a
    /// specified regex.
    indirect case regexExpectation(DataQualityRule.RegexExpectation?)
    /// Row-level rule which evaluates whether each column value is unique.
    indirect case uniquenessExpectation(DataQualityRule.UniquenessExpectation?)
    /// Aggregate rule which evaluates whether the column aggregate
    /// statistic lies between a specified range.
    indirect case statisticRangeExpectation(DataQualityRule.StatisticRangeExpectation?)
    /// Row-level rule which evaluates whether each row in a table passes the
    /// specified condition.
    indirect case rowConditionExpectation(DataQualityRule.RowConditionExpectation?)
    /// Aggregate rule which evaluates whether the provided expression is true
    /// for a table.
    indirect case tableConditionExpectation(DataQualityRule.TableConditionExpectation?)
    /// Aggregate rule which evaluates the number of rows returned for the
    /// provided statement. If any rows are returned, this rule fails.
    indirect case sqlAssertion(DataQualityRule.SqlAssertion?)
    /// Aggregate rule which references a rule template and provides the
    /// parameters to be substituted in the template. If any rows are returned,
    /// this rule fails.
    indirect case templateReference(DataQualityRule.TemplateReference?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.DataQualityRule"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
