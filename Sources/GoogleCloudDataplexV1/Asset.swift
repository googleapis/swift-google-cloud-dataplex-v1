// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// An asset represents a cloud resource that is being managed within a lake as a
/// member of a zone.
public struct Asset: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Output only. The relative resource name of the asset, of the form:
  /// `projects/{project_number}/locations/{location_id}/lakes/{lake_id}/zones/{zone_id}/assets/{asset_id}`.
  public var name: Swift.String = Swift.String()

  /// Optional. User friendly display name.
  public var displayName: Swift.String = Swift.String()

  /// Output only. System generated globally unique ID for the asset. This ID
  /// will be different if the asset is deleted and re-created with the same
  /// name.
  public var uid: Swift.String = Swift.String()

  /// Output only. The time when the asset was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The time when the asset was last updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// Optional. User defined labels for the asset.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Optional. Description of the asset.
  public var description: Swift.String = Swift.String()

  /// Output only. Current state of the asset.
  public var state: State = State()

  /// Required. Specification of the resource that is referenced by this asset.
  public var resourceSpec: Asset.ResourceSpec? = nil

  /// Output only. Status of the resource referenced by this asset.
  public var resourceStatus: Asset.ResourceStatus? = nil

  /// Output only. Status of the security policy applied to resource referenced
  /// by this asset.
  public var securityStatus: Asset.SecurityStatus? = nil

  /// Optional. Specification of the discovery feature applied to data referenced
  /// by this asset. When this spec is left unset, the asset will use the spec
  /// set on the parent zone.
  public var discoverySpec: Asset.DiscoverySpec? = nil

  /// Output only. Status of the discovery feature applied to data referenced by
  /// this asset.
  public var discoveryStatus: Asset.DiscoveryStatus? = nil

  /// Initialize a new instance of `Asset`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Asset().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Security policy status of the asset. Data security policy, i.e., readers,
  /// writers & owners, should be specified in the lake/zone/asset IAM policy.
  public struct SecurityStatus: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The current state of the security policy applied to the attached
    /// resource.
    public var state: Asset.SecurityStatus.State = Asset.SecurityStatus.State()

    /// Additional information about the current state.
    public var message: Swift.String = Swift.String()

    /// Last update time of the status.
    public var updateTime: GoogleCloudWKT.Timestamp? = nil

    /// Initialize a new instance of `SecurityStatus`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SecurityStatus().with { $0.state = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The state of the security policy.
    public enum State: Codable, Equatable, Sendable {
      /// State unspecified.
      case unspecified
      /// Security policy has been successfully applied to the attached resource.
      case ready
      /// Security policy is in the process of being applied to the attached
      /// resource.
      case applying
      /// Security policy could not be applied to the attached resource due to
      /// errors.
      case error
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .ready: return 1
        case .applying: return 2
        case .error: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .ready: return "READY"
        case .applying: return "APPLYING"
        case .error: return "ERROR"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "READY": self = .ready
        case "APPLYING": self = .applying
        case "ERROR": self = .error
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .ready
        case 2: self = .applying
        case 3: self = .error
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .ready: return try container.encode(1)
        case .applying: return try container.encode(2)
        case .error: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Asset.SecurityStatus"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Settings to manage the metadata discovery and publishing for an asset.
  public struct DiscoverySpec: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. Whether discovery is enabled.
    public var enabled: Swift.Bool = Swift.Bool()

    /// Optional. The list of patterns to apply for selecting data to include
    /// during discovery if only a subset of the data should considered.  For
    /// Cloud Storage bucket assets, these are interpreted as glob patterns used
    /// to match object names. For BigQuery dataset assets, these are interpreted
    /// as patterns to match table names.
    public var includePatterns: [Swift.String] = []

    /// Optional. The list of patterns to apply for selecting data to exclude
    /// during discovery.  For Cloud Storage bucket assets, these are interpreted
    /// as glob patterns used to match object names. For BigQuery dataset assets,
    /// these are interpreted as patterns to match table names.
    public var excludePatterns: [Swift.String] = []

    /// Optional. Configuration for CSV data.
    public var csvOptions: Asset.DiscoverySpec.CsvOptions? = nil

    /// Optional. Configuration for Json data.
    public var jsonOptions: Asset.DiscoverySpec.JsonOptions? = nil

    /// Determines when discovery is triggered.
    public var trigger: OneOf_Trigger? = nil

    /// Initialize a new instance of `DiscoverySpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DiscoverySpec().with { $0.enabled = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case enabled = "enabled"
      case includePatterns = "includePatterns"
      case excludePatterns = "excludePatterns"
      case csvOptions = "csvOptions"
      case jsonOptions = "jsonOptions"
      case schedule = "schedule"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.enabled = try container.decode(Swift.Bool.self, forKey: .enabled)
      self.includePatterns = try container.decode([Swift.String].self, forKey: .includePatterns)
      self.excludePatterns = try container.decode([Swift.String].self, forKey: .excludePatterns)
      self.csvOptions = try container.decodeIfPresent(
        Asset.DiscoverySpec.CsvOptions.self, forKey: .csvOptions)
      self.jsonOptions = try container.decodeIfPresent(
        Asset.DiscoverySpec.JsonOptions.self, forKey: .jsonOptions)

      var trigger: OneOf_Trigger? = nil
      let triggerCheckAndSet = {
        if trigger != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'trigger'"))
        }
        trigger = $0
      }
      if let schedule = try container.decodeIfPresent(Swift.String.self, forKey: .schedule) {
        try triggerCheckAndSet(.schedule(schedule))
      }
      self.trigger = trigger
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.enabled, forKey: .enabled)
      try container.encode(self.includePatterns, forKey: .includePatterns)
      try container.encode(self.excludePatterns, forKey: .excludePatterns)
      try container.encode(self.csvOptions, forKey: .csvOptions)
      try container.encode(self.jsonOptions, forKey: .jsonOptions)

      if let choice = self.trigger {
        switch choice {
        case .schedule(let value):
          try container.encode(value, forKey: .schedule)
        }
      }
    }

    /// Describe CSV and similar semi-structured data formats.
    public struct CsvOptions: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Optional. The number of rows to interpret as header rows that should be
      /// skipped when reading data rows.
      public var headerRows: Swift.Int32 = Swift.Int32()

      /// Optional. The delimiter being used to separate values. This defaults to
      /// ','.
      public var delimiter: Swift.String = Swift.String()

      /// Optional. The character encoding of the data. The default is UTF-8.
      public var encoding: Swift.String = Swift.String()

      /// Optional. Whether to disable the inference of data type for CSV data.
      /// If true, all columns will be registered as strings.
      public var disableTypeInference: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `CsvOptions`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CsvOptions().with { $0.headerRows = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dataplex.v1.Asset.DiscoverySpec.CsvOptions"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Describe JSON data format.
    public struct JsonOptions: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Optional. The character encoding of the data. The default is UTF-8.
      public var encoding: Swift.String = Swift.String()

      /// Optional. Whether to disable the inference of data type for Json data.
      /// If true, all columns will be registered as their primitive types
      /// (strings, number or boolean).
      public var disableTypeInference: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `JsonOptions`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = JsonOptions().with { $0.encoding = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dataplex.v1.Asset.DiscoverySpec.JsonOptions"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Determines when discovery is triggered.
    public enum OneOf_Trigger: Codable, Equatable, Sendable {
      /// Optional. Cron schedule (https://en.wikipedia.org/wiki/Cron) for
      /// running discovery periodically. Successive discovery runs must be
      /// scheduled at least 60 minutes apart. The default value is to run
      /// discovery every 60 minutes.
      ///
      /// To explicitly set a timezone to the cron tab, apply a prefix in the
      /// cron tab: "CRON_TZ=${IANA_TIME_ZONE}" or TZ=${IANA_TIME_ZONE}".
      /// The ${IANA_TIME_ZONE} may only be a valid string from IANA time zone
      /// database. For example, `CRON_TZ=America/New_York 1 * * * *`, or
      /// `TZ=America/New_York 1 * * * *`.
      case schedule(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Asset.DiscoverySpec"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Identifies the cloud resource that is referenced by this asset.
  public struct ResourceSpec: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Immutable. Relative name of the cloud resource that contains the data
    /// that is being managed within a lake. For example:
    ///   `projects/{project_number}/buckets/{bucket_id}`
    ///   `projects/{project_number}/datasets/{dataset_id}`
    public var name: Swift.String = Swift.String()

    /// Required. Immutable. Type of resource.
    public var type: Asset.ResourceSpec.Type_ = Asset.ResourceSpec.Type_()

    /// Optional. Determines how read permissions are handled for each asset and
    /// their associated tables. Only available to storage buckets assets.
    public var readAccessMode: Asset.ResourceSpec.AccessMode = Asset.ResourceSpec.AccessMode()

    /// Initialize a new instance of `ResourceSpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ResourceSpec().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Type of resource.
    public enum Type_: Codable, Equatable, Sendable {
      /// Type not specified.
      case unspecified
      /// Cloud Storage bucket.
      case storageBucket
      /// BigQuery dataset.
      case bigqueryDataset
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .storageBucket: return 1
        case .bigqueryDataset: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TYPE_UNSPECIFIED"
        case .storageBucket: return "STORAGE_BUCKET"
        case .bigqueryDataset: return "BIGQUERY_DATASET"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TYPE_UNSPECIFIED": self = .unspecified
        case "STORAGE_BUCKET": self = .storageBucket
        case "BIGQUERY_DATASET": self = .bigqueryDataset
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .storageBucket
        case 2: self = .bigqueryDataset
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .storageBucket: return try container.encode(1)
        case .bigqueryDataset: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Access Mode determines how data stored within the resource is read. This
    /// is only applicable to storage bucket assets.
    public enum AccessMode: Codable, Equatable, Sendable {
      /// Access mode unspecified.
      case unspecified
      /// Default. Data is accessed directly using storage APIs.
      case direct
      /// Data is accessed through a managed interface using BigQuery APIs.
      case managed
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .direct: return 1
        case .managed: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "ACCESS_MODE_UNSPECIFIED"
        case .direct: return "DIRECT"
        case .managed: return "MANAGED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:AccessMode/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "ACCESS_MODE_UNSPECIFIED": self = .unspecified
        case "DIRECT": self = .direct
        case "MANAGED": self = .managed
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:AccessMode/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .direct
        case 2: self = .managed
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .direct: return try container.encode(1)
        case .managed: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Asset.ResourceSpec"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Status of the resource referenced by an asset.
  public struct ResourceStatus: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The current state of the managed resource.
    public var state: Asset.ResourceStatus.State = Asset.ResourceStatus.State()

    /// Additional information about the current state.
    public var message: Swift.String = Swift.String()

    /// Last update time of the status.
    public var updateTime: GoogleCloudWKT.Timestamp? = nil

    /// Output only. Service account associated with the BigQuery Connection.
    public var managedAccessIdentity: Swift.String = Swift.String()

    /// Initialize a new instance of `ResourceStatus`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ResourceStatus().with { $0.state = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The state of a resource.
    public enum State: Codable, Equatable, Sendable {
      /// State unspecified.
      case unspecified
      /// Resource does not have any errors.
      case ready
      /// Resource has errors.
      case error
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .ready: return 1
        case .error: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .ready: return "READY"
        case .error: return "ERROR"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "READY": self = .ready
        case "ERROR": self = .error
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .ready
        case 2: self = .error
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .ready: return try container.encode(1)
        case .error: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Asset.ResourceStatus"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Status of discovery for an asset.
  public struct DiscoveryStatus: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The current status of the discovery feature.
    public var state: Asset.DiscoveryStatus.State = Asset.DiscoveryStatus.State()

    /// Additional information about the current state.
    public var message: Swift.String = Swift.String()

    /// Last update time of the status.
    public var updateTime: GoogleCloudWKT.Timestamp? = nil

    /// The start time of the last discovery run.
    public var lastRunTime: GoogleCloudWKT.Timestamp? = nil

    /// Data Stats of the asset reported by discovery.
    public var stats: Asset.DiscoveryStatus.Stats? = nil

    /// The duration of the last discovery run.
    public var lastRunDuration: GoogleCloudWKT.Duration? = nil

    /// Initialize a new instance of `DiscoveryStatus`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DiscoveryStatus().with { $0.state = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The aggregated data statistics for the asset reported by discovery.
    public struct Stats: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// The count of data items within the referenced resource.
      public var dataItems: Swift.Int64 = Swift.Int64()

      /// The number of stored data bytes within the referenced resource.
      public var dataSize: Swift.Int64 = Swift.Int64()

      /// The count of table entities within the referenced resource.
      public var tables: Swift.Int64 = Swift.Int64()

      /// The count of fileset entities within the referenced resource.
      public var filesets: Swift.Int64 = Swift.Int64()

      /// Initialize a new instance of `Stats`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Stats().with { $0.dataItems = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dataplex.v1.Asset.DiscoveryStatus.Stats"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// Current state of discovery.
    public enum State: Codable, Equatable, Sendable {
      /// State is unspecified.
      case unspecified
      /// Discovery for the asset is scheduled.
      case scheduled
      /// Discovery for the asset is running.
      case inProgress
      /// Discovery for the asset is currently paused (e.g. due to a lack
      /// of available resources). It will be automatically resumed.
      case paused
      /// Discovery for the asset is disabled.
      case disabled
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .scheduled: return 1
        case .inProgress: return 2
        case .paused: return 3
        case .disabled: return 5
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .scheduled: return "SCHEDULED"
        case .inProgress: return "IN_PROGRESS"
        case .paused: return "PAUSED"
        case .disabled: return "DISABLED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "SCHEDULED": self = .scheduled
        case "IN_PROGRESS": self = .inProgress
        case "PAUSED": self = .paused
        case "DISABLED": self = .disabled
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .scheduled
        case 2: self = .inProgress
        case 3: self = .paused
        case 5: self = .disabled
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .scheduled: return try container.encode(1)
        case .inProgress: return try container.encode(2)
        case .paused: return try container.encode(3)
        case .disabled: return try container.encode(5)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Asset.DiscoveryStatus"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.Asset"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
