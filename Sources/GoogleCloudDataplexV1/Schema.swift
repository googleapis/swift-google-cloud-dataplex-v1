// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Schema information describing the structure and layout of the data.
public struct Schema: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Required. Set to `true` if user-managed or `false` if managed by Dataplex
  /// Universal Catalog. The default is `false` (managed by Dataplex Universal
  /// Catalog).
  ///
  /// - Set to `false`to enable Dataplex Universal Catalog discovery to update
  /// the schema.
  ///   including new data discovery, schema inference, and schema evolution.
  ///   Users retain the ability to input and edit the schema. Dataplex Universal
  ///   Catalog treats schema input by the user as though produced by a previous
  ///   Dataplex Universal Catalog discovery operation, and it will evolve the
  ///   schema and take action based on that treatment.
  ///
  /// - Set to `true` to fully manage the entity
  ///   schema. This setting guarantees that Dataplex Universal Catalog will not
  ///   change schema fields.
  public var userManaged: Swift.Bool = Swift.Bool()

  /// Optional. The sequence of fields describing data in table entities.
  /// **Note:** BigQuery SchemaFields are immutable.
  public var fields: [Schema.SchemaField] = []

  /// Optional. The sequence of fields describing the partition structure in
  /// entities. If this field is empty, there are no partitions within the data.
  public var partitionFields: [Schema.PartitionField] = []

  /// Optional. The structure of paths containing partition data within the
  /// entity.
  public var partitionStyle: Schema.PartitionStyle = Schema.PartitionStyle()

  /// Initialize a new instance of `Schema`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Schema().with { $0.userManaged = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Represents a column field within a table schema.
  public struct SchemaField: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. The name of the field. Must contain only letters, numbers and
    /// underscores, with a maximum length of 767 characters,
    /// and must begin with a letter or underscore.
    public var name: Swift.String = Swift.String()

    /// Optional. User friendly field description. Must be less than or equal to
    /// 1024 characters.
    public var description: Swift.String = Swift.String()

    /// Required. The type of field.
    public var type: Schema.Type_ = Schema.Type_()

    /// Required. Additional field semantics.
    public var mode: Schema.Mode = Schema.Mode()

    /// Optional. Any nested field for complex types.
    public var fields: [Schema.SchemaField] = []

    /// Initialize a new instance of `SchemaField`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SchemaField().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Schema.SchemaField"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Represents a key field within the entity's partition structure. You could
  /// have up to 20 partition fields, but only the first 10 partitions have the
  /// filtering ability due to performance consideration. **Note:**
  /// Partition fields are immutable.
  public struct PartitionField: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Required. Partition field name must consist of letters, numbers, and
    /// underscores only, with a maximum of length of 256 characters, and must
    /// begin with a letter or underscore..
    public var name: Swift.String = Swift.String()

    /// Required. Immutable. The type of field.
    public var type: Schema.Type_ = Schema.Type_()

    /// Initialize a new instance of `PartitionField`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PartitionField().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.Schema.PartitionField"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Type information for fields in schemas and partition schemas.
  public enum Type_: Codable, Equatable, Sendable {
    /// SchemaType unspecified.
    case unspecified
    /// Boolean field.
    case boolean
    /// Single byte numeric field.
    case byte
    /// 16-bit numeric field.
    case int16
    /// 32-bit numeric field.
    case int32
    /// 64-bit numeric field.
    case int64
    /// Floating point numeric field.
    case float
    /// Double precision numeric field.
    case double
    /// Real value numeric field.
    case decimal
    /// Sequence of characters field.
    case string
    /// Sequence of bytes field.
    case binary
    /// Date and time field.
    case timestamp
    /// Date field.
    case date
    /// Time field.
    case time
    /// Structured field. Nested fields that define the structure of the map.
    /// If all nested fields are nullable, this field represents a union.
    case record
    /// Null field that does not have values.
    case null
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .boolean: return 1
      case .byte: return 2
      case .int16: return 3
      case .int32: return 4
      case .int64: return 5
      case .float: return 6
      case .double: return 7
      case .decimal: return 8
      case .string: return 9
      case .binary: return 10
      case .timestamp: return 11
      case .date: return 12
      case .time: return 13
      case .record: return 14
      case .null: return 100
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "TYPE_UNSPECIFIED"
      case .boolean: return "BOOLEAN"
      case .byte: return "BYTE"
      case .int16: return "INT16"
      case .int32: return "INT32"
      case .int64: return "INT64"
      case .float: return "FLOAT"
      case .double: return "DOUBLE"
      case .decimal: return "DECIMAL"
      case .string: return "STRING"
      case .binary: return "BINARY"
      case .timestamp: return "TIMESTAMP"
      case .date: return "DATE"
      case .time: return "TIME"
      case .record: return "RECORD"
      case .null: return "NULL"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "TYPE_UNSPECIFIED": self = .unspecified
      case "BOOLEAN": self = .boolean
      case "BYTE": self = .byte
      case "INT16": self = .int16
      case "INT32": self = .int32
      case "INT64": self = .int64
      case "FLOAT": self = .float
      case "DOUBLE": self = .double
      case "DECIMAL": self = .decimal
      case "STRING": self = .string
      case "BINARY": self = .binary
      case "TIMESTAMP": self = .timestamp
      case "DATE": self = .date
      case "TIME": self = .time
      case "RECORD": self = .record
      case "NULL": self = .null
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .boolean
      case 2: self = .byte
      case 3: self = .int16
      case 4: self = .int32
      case 5: self = .int64
      case 6: self = .float
      case 7: self = .double
      case 8: self = .decimal
      case 9: self = .string
      case 10: self = .binary
      case 11: self = .timestamp
      case 12: self = .date
      case 13: self = .time
      case 14: self = .record
      case 100: self = .null
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .boolean: return try container.encode(1)
      case .byte: return try container.encode(2)
      case .int16: return try container.encode(3)
      case .int32: return try container.encode(4)
      case .int64: return try container.encode(5)
      case .float: return try container.encode(6)
      case .double: return try container.encode(7)
      case .decimal: return try container.encode(8)
      case .string: return try container.encode(9)
      case .binary: return try container.encode(10)
      case .timestamp: return try container.encode(11)
      case .date: return try container.encode(12)
      case .time: return try container.encode(13)
      case .record: return try container.encode(14)
      case .null: return try container.encode(100)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Additional qualifiers to define field semantics.
  public enum Mode: Codable, Equatable, Sendable {
    /// Mode unspecified.
    case unspecified
    /// The field has required semantics.
    case `required`
    /// The field has optional semantics, and may be null.
    case nullable
    /// The field has repeated (0 or more) semantics, and is a list of values.
    case repeated
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .`required`: return 1
      case .nullable: return 2
      case .repeated: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "MODE_UNSPECIFIED"
      case .`required`: return "REQUIRED"
      case .nullable: return "NULLABLE"
      case .repeated: return "REPEATED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Mode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "MODE_UNSPECIFIED": self = .unspecified
      case "REQUIRED": self = .`required`
      case "NULLABLE": self = .nullable
      case "REPEATED": self = .repeated
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Mode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .`required`
      case 2: self = .nullable
      case 3: self = .repeated
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .`required`: return try container.encode(1)
      case .nullable: return try container.encode(2)
      case .repeated: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The structure of paths within the entity, which represent partitions.
  public enum PartitionStyle: Codable, Equatable, Sendable {
    /// PartitionStyle unspecified
    case unspecified
    /// Partitions are hive-compatible.
    /// Examples: `gs://bucket/path/to/table/dt=2019-10-31/lang=en`,
    /// `gs://bucket/path/to/table/dt=2019-10-31/lang=en/late`.
    case hiveCompatible
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .hiveCompatible: return 1
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PARTITION_STYLE_UNSPECIFIED"
      case .hiveCompatible: return "HIVE_COMPATIBLE"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:PartitionStyle/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PARTITION_STYLE_UNSPECIFIED": self = .unspecified
      case "HIVE_COMPATIBLE": self = .hiveCompatible
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:PartitionStyle/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .hiveCompatible
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .hiveCompatible: return try container.encode(1)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.Schema"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
