// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// The identity to run the datascan.
public struct ExecutionIdentity: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The identity to run the datascan.
  public var identity: OneOf_Identity? = nil

  /// Initialize a new instance of `ExecutionIdentity`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ExecutionIdentity().with { $0.dataplexServiceAgent = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case dataplexServiceAgent = "dataplexServiceAgent"
    case userCredential = "userCredential"
    case serviceAccount = "serviceAccount"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var identity: OneOf_Identity? = nil
    let identityCheckAndSet = {
      if identity != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'identity'"))
      }
      identity = $0
    }
    if let dataplexServiceAgent = try container.decodeIfPresent(
      ExecutionIdentity.DataplexServiceAgent?.self, forKey: .dataplexServiceAgent)
    {
      try identityCheckAndSet(.dataplexServiceAgent(dataplexServiceAgent))
    }
    if let userCredential = try container.decodeIfPresent(
      ExecutionIdentity.UserCredential?.self, forKey: .userCredential)
    {
      try identityCheckAndSet(.userCredential(userCredential))
    }
    if let serviceAccount = try container.decodeIfPresent(
      ExecutionIdentity.ServiceAccount?.self, forKey: .serviceAccount)
    {
      try identityCheckAndSet(.serviceAccount(serviceAccount))
    }
    self.identity = identity
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.identity {
      switch choice {
      case .dataplexServiceAgent(let value):
        try container.encode(value, forKey: .dataplexServiceAgent)
      case .userCredential(let value):
        try container.encode(value, forKey: .userCredential)
      case .serviceAccount(let value):
        try container.encode(value, forKey: .serviceAccount)
      }
    }
  }

  /// The Dataplex service agent associated with the user's project.
  public struct DataplexServiceAgent: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `DataplexServiceAgent`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DataplexServiceAgent().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.ExecutionIdentity.DataplexServiceAgent"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The credential of the calling user.
  public struct UserCredential: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `UserCredential`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = UserCredential().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.ExecutionIdentity.UserCredential"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The service account
  public struct ServiceAccount: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Required. Service account email. The datascan will execute with this
    /// service account's credentials. The user calling this API must have
    /// permissions to act as this service account. Dataplex service agent must
    /// be granted iam.serviceAccounts.getAccessToken permission on this service
    /// account, for example, through the iam.serviceAccountTokenCreator role .
    public var email: Swift.String = Swift.String()

    /// Initialize a new instance of `ServiceAccount`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ServiceAccount().with { $0.email = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.ExecutionIdentity.ServiceAccount"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The identity to run the datascan.
  public enum OneOf_Identity: Codable, Equatable, Sendable {
    /// Optional. The Dataplex service agent associated with the user's project.
    indirect case dataplexServiceAgent(ExecutionIdentity.DataplexServiceAgent?)
    /// Optional. The credential of the calling user. Supports only ONE_TIME
    /// trigger type.
    indirect case userCredential(ExecutionIdentity.UserCredential?)
    /// Optional. The provided service account.
    indirect case serviceAccount(ExecutionIdentity.ServiceAccount?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.ExecutionIdentity"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
