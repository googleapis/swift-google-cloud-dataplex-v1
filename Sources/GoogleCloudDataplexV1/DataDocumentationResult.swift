// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// The output of a DataDocumentation scan.
public struct DataDocumentationResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The result of the data documentation scan.
  public var result: OneOf_Result? = nil

  /// Initialize a new instance of `DataDocumentationResult`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = DataDocumentationResult().with { $0.datasetResult = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case datasetResult = "datasetResult"
    case tableResult = "tableResult"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var result: OneOf_Result? = nil
    let resultCheckAndSet = {
      if result != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'result'"))
      }
      result = $0
    }
    if let datasetResult = try container.decodeIfPresent(
      DataDocumentationResult.DatasetResult?.self, forKey: .datasetResult)
    {
      try resultCheckAndSet(.datasetResult(datasetResult))
    }
    if let tableResult = try container.decodeIfPresent(
      DataDocumentationResult.TableResult?.self, forKey: .tableResult)
    {
      try resultCheckAndSet(.tableResult(tableResult))
    }
    self.result = result
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.result {
      switch choice {
      case .datasetResult(let value):
        try container.encode(value, forKey: .datasetResult)
      case .tableResult(let value):
        try container.encode(value, forKey: .tableResult)
      }
    }
  }

  /// Insights for a dataset resource.
  public struct DatasetResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. Generated Dataset description.
    public var overview: Swift.String = Swift.String()

    /// Output only. Relationships suggesting how tables in the dataset are
    /// related to each other, based on their schema.
    public var schemaRelationships: [DataDocumentationResult.SchemaRelationship] = []

    /// Output only. Sample SQL queries for the dataset.
    public var queries: [DataDocumentationResult.Query] = []

    /// Initialize a new instance of `DatasetResult`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DatasetResult().with { $0.overview = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.DatasetResult"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Insights for a table resource.
  public struct TableResult: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. The service-qualified full resource name of the cloud
    /// resource. Ex:
    /// //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID
    public var name: Swift.String = Swift.String()

    /// Output only. Generated description of the table.
    public var overview: Swift.String = Swift.String()

    /// Output only. Schema of the table with generated metadata of the columns
    /// in the schema.
    public var schema: DataDocumentationResult.Schema? = nil

    /// Output only. Sample SQL queries for the table.
    public var queries: [DataDocumentationResult.Query] = []

    /// Initialize a new instance of `TableResult`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = TableResult().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.TableResult"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Details of the relationship between the schema of two resources.
  public struct SchemaRelationship: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. An ordered list of fields for the join from the first table.
    /// The size of this list must be the same as `right_schema_paths`.
    /// Each field at index i in this list must correspond to a field at the same
    /// index in the `right_schema_paths` list.
    public var leftSchemaPaths: DataDocumentationResult.SchemaRelationship.SchemaPaths? = nil

    /// Output only. An ordered list of fields for the join from the second
    /// table. The size of this list must be the same as `left_schema_paths`.
    /// Each field at index i in this list must correspond to a field at the same
    /// index in the `left_schema_paths` list.
    public var rightSchemaPaths: DataDocumentationResult.SchemaRelationship.SchemaPaths? = nil

    /// Output only. Sources which generated the schema relation edge.
    public var sources: [DataDocumentationResult.SchemaRelationship.Source] = []

    /// Output only. The type of relationship between the schema paths.
    public var type: DataDocumentationResult.SchemaRelationship.Type_ = DataDocumentationResult
      .SchemaRelationship.Type_()

    /// Initialize a new instance of `SchemaRelationship`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SchemaRelationship().with { $0.leftSchemaPaths = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Represents an ordered set of paths within a table's schema.
    public struct SchemaPaths: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Output only. The service-qualified full resource name of the table
      /// Ex:
      /// //bigquery.googleapis.com/projects/PROJECT_ID/datasets/DATASET_ID/tables/TABLE_ID
      public var tableFqn: Swift.String = Swift.String()

      /// Output only. An ordered set of Paths to fields within the schema of the
      /// table. For fields nested within a top level field of type record, use
      /// '.' to separate field names. Examples: Top level field - `top_level`
      /// Nested field - `top_level.child.sub_field`
      public var paths: [Swift.String] = []

      /// Initialize a new instance of `SchemaPaths`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = SchemaPaths().with { $0.tableFqn = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.SchemaRelationship.SchemaPaths"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Source which generated the schema relation edge.
    public enum Source: Codable, Equatable, Sendable {
      /// The source of the schema relationship is unspecified.
      case unspecified
      /// The source of the schema relationship is agent.
      case agent
      /// The source of the schema relationship is query history from the source
      /// system.
      case queryHistory
      /// The source of the schema relationship is table constraints added in
      /// the source system.
      case tableConstraints
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .agent: return 4
        case .queryHistory: return 5
        case .tableConstraints: return 6
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SOURCE_UNSPECIFIED"
        case .agent: return "AGENT"
        case .queryHistory: return "QUERY_HISTORY"
        case .tableConstraints: return "TABLE_CONSTRAINTS"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SOURCE_UNSPECIFIED": self = .unspecified
        case "AGENT": self = .agent
        case "QUERY_HISTORY": self = .queryHistory
        case "TABLE_CONSTRAINTS": self = .tableConstraints
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 4: self = .agent
        case 5: self = .queryHistory
        case 6: self = .tableConstraints
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .agent: return try container.encode(4)
        case .queryHistory: return try container.encode(5)
        case .tableConstraints: return try container.encode(6)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// The type of relationship.
    public enum Type_: Codable, Equatable, Sendable {
      /// The type of the schema relationship is unspecified.
      case unspecified
      /// Indicates a join relationship between the schema fields.
      case schemaJoin
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .schemaJoin: return 1
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TYPE_UNSPECIFIED"
        case .schemaJoin: return "SCHEMA_JOIN"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TYPE_UNSPECIFIED": self = .unspecified
        case "SCHEMA_JOIN": self = .schemaJoin
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .schemaJoin
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .schemaJoin: return try container.encode(1)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.SchemaRelationship"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// A sample SQL query in data documentation.
  public struct Query: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. The SQL query string which can be executed.
    public var sql: Swift.String = Swift.String()

    /// Output only. The description for the query.
    public var description: Swift.String = Swift.String()

    /// Initialize a new instance of `Query`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Query().with { $0.sql = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.Query"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Schema of the table with generated metadata of columns.
  public struct Schema: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. The list of columns.
    public var fields: [DataDocumentationResult.Field] = []

    /// Initialize a new instance of `Schema`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Schema().with { $0.fields = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.Schema"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Column of a table with generated metadata and nested fields.
  public struct Field: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Output only. The name of the column.
    public var name: Swift.String = Swift.String()

    /// Output only. Generated description for columns and fields.
    public var description: Swift.String = Swift.String()

    /// Output only. Nested fields.
    public var fields: [DataDocumentationResult.Field] = []

    /// Initialize a new instance of `Field`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Field().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult.Field"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The result of the data documentation scan.
  public enum OneOf_Result: Codable, Equatable, Sendable {
    /// Output only. Insights for a Dataset resource.
    indirect case datasetResult(DataDocumentationResult.DatasetResult?)
    /// Output only. Insights for a Table resource.
    indirect case tableResult(DataDocumentationResult.TableResult?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataplex.v1.DataDocumentationResult"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
